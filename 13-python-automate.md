# 13. Python Automate 실무 자동화

파일·웹·표·문서·예약 실행의 반복 작업을 Python으로 자동화하고, 파일 무결성 확인과 보안 점검 보고서 작성에 적용하는 보충 챕터입니다. 이 챕터는 01~12장의 핵심 과정에 포함되지 않는 **기타 챕터**로 독립적으로 운영합니다.

{% hint style="info" %}
## 🧭 학습 목표

- 자동화할 수 있는 반복 작업을 입력·변환·출력으로 분해합니다.
- 미리보기, 재실행 안전성, 로그, 원상복구를 설계합니다.
- 파일·웹·스프레드시트·문서 자동화 도구를 적용합니다.
- 예약 실행과 알림에서 비밀값·장애·중복 실행을 처리합니다.
- 파일 변경 탐지와 보안 점검 결과 보고서 프로젝트를 완성합니다.
{% endhint %}

## 참고 교재 반영 범위

첨부된 *Automate the Boring Stuff with Python* 참고 교재의 Part II 주제 체계를 기준으로, 현재 교안에 맞게 다음처럼 재구성했습니다.

| 참고 주제 | 이 챕터의 반영 방식 | 현대화 포인트 |
| --- | --- | --- |
| 파일 읽기·정리·압축 | 13-2, 프로젝트 A | `pathlib`, 원본 보존, SHA-256 기준선 비교 |
| 웹 스크래핑 | 13-3 | 타임아웃, 응답 크기 제한, 이용 약관·수집 범위 |
| Excel 스프레드시트 | 13-4, 프로젝트 B | `openpyxl`, 자료형 유지, 검증 오류 보존 |
| PDF·Word 문서 | 13-4 | `pypdf`, `python-docx`, 원본 보존 |
| 시간·예약 실행 | 13-5 | 운영체제 스케줄러, 멱등성, 실행 상태 |
| 메일·메시지 | 13-5 | 미리보기, 환경 변수, 전송 제한 |
| 이미지·GUI 제어 | 13-5 | Pillow, 좌표 자동화의 한계, 중단 장치 |

04·05·07·08·09장과 겹치는 문법과 기초 API는 반복하지 않고, 업무 흐름·실패 복구·실전 프로젝트에 집중합니다.

## 선행 지식

- 04장: 경로, 파일, CSV·JSON, 오류 처리
- 05장: 문자열·정규표현식·날짜·데이터 검증
- 07장: HTTP 요청, 타임아웃, 응답 검증
- 08장: CLI, 환경 변수, 설정, 로깅과 필요한 경우의 외부 프로세스 실행
- 09장: pytest와 실패 재현

## 학습 순서

1. [13-1. 자동화 과제 선정과 안전 설계](13-python-automate/13-1-automation-design.md)
2. [13-2. 파일과 폴더 일괄 처리](13-python-automate/13-2-file-batch.md)
3. [13-3. 웹 정보 수집](13-python-automate/13-3-web-collection.md)
4. [13-4. 스프레드시트와 문서 자동화](13-python-automate/13-4-spreadsheet-documents.md)
5. [13-5. 예약·알림·이미지·GUI 자동화](13-python-automate/13-5-scheduling-gui.md)
6. [13-6. 프로젝트 A - 파일 무결성 변경 탐지기](13-python-automate/13-6-safe-file-organizer-project.md)
7. [13-7. 프로젝트 B - 보안 점검 결과 보고서](13-python-automate/13-7-spreadsheet-report-project.md)

## 실습 환경

핵심 과정 의존성과 보충 챕터용 의존성을 분리했습니다.

```bash
python -m venv .venv
source .venv/bin/activate          # Windows: .venv\Scripts\activate
python -m pip install -r requirements-automate.txt
```

GUI 자동화가 필요하지 않으면 `PyAutoGUI`는 설치 항목에서 제외해도 됩니다.

## 종합 프로젝트

### 프로젝트 A: 파일 무결성 변경 탐지기

학습용 설정 폴더의 상대 경로와 SHA-256을 기준선으로 저장하고 추가·수정·삭제를 탐지합니다. 검사 실패와 변경 발견을 구분하며 원본 파일을 보존합니다.

### 프로젝트 B: 보안 점검 결과 보고서

합성 보안 점검 CSV를 검증하고 양호·미흡·검토 필요·해당 없음을 집계합니다. 점검 결과와 입력 오류를 별도 시트에 보존하고 원본 CSV 해시를 기록합니다.

## 자동화 공통 안전 규칙

1. 원본은 읽기 전용으로 다루고 출력은 다른 경로에 만듭니다.
2. 삭제보다 이동, 이동보다 미리보기를 우선합니다.
3. 동일한 입력으로 다시 실행해도 중복 처리나 손상이 없어야 합니다.
4. 네트워크와 외부 서비스에는 타임아웃·횟수·크기 제한을 둡니다.
5. 비밀번호와 API 키는 코드·CSV·로그에 저장하지 않습니다.
6. 파괴적 작업과 외부 전송은 사람의 명시적 승인 단계를 거칩니다.

{% hint style="success" %}
## ✅ 완료 기준

- [ ] 자동화 대상을 입력·규칙·출력·실패 기준으로 정의했습니다.
- [ ] SHA-256 기준선으로 파일 추가·수정·삭제를 구분할 수 있습니다.
- [ ] 변경 발견과 검사 실패의 종료 코드를 구분할 수 있습니다.
- [ ] 보안 점검 결과와 입력 오류를 분리해 Excel 보고서를 생성할 수 있습니다.
- [ ] 예약 실행에서 중복 실행·비밀값·장애를 처리할 수 있습니다.
- [ ] 정상·오류·경계 입력 테스트가 통과합니다.
{% endhint %}

---

첫 세부 단원: [13-1. 자동화 과제 선정과 안전 설계](13-python-automate/13-1-automation-design.md)
