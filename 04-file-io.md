# 04. 파일 입출력과 데이터 형식

프로그램의 데이터는 실행이 끝나면 메모리에서 사라진다. 파일은 데이터를 다음 실행까지 보존하고 다른 프로그램과 교환하는 가장 기본적인 수단이다. 이 장에서는 허용된 작업 범위 안에서 파일을 안전하게 다루고, 텍스트·바이너리·CSV·JSON·JSON Lines 형식을 목적에 맞게 처리한다. 마지막에는 앞 절의 기능을 연결해 원본을 변경하지 않는 파일 분석기를 완성한다.

{% hint style="info" %}
### 🧭 학습 목표

- `pathlib`로 경로를 구성하고 허용된 작업 범위를 검증한다.
- 파일과 디렉터리를 생성·복사·이동·삭제한다.
- 텍스트와 바이너리 파일을 목적에 맞는 모드로 읽고 쓴다.
- 인코딩과 바이트 순서를 고려해 텍스트와 바이너리 구조를 해석한다.
- CSV·JSON·JSON Lines 데이터를 읽고 검증해 안전하게 저장한다.
- 큰 파일을 한 줄씩 처리하고 입력 스트리밍과 전체 메모리 사용을 구분한다.
- 대표적인 파일 헤더와 메타데이터를 이용해 파일 형식을 분석한다.
- 고유한 임시 파일과 원자적 교체로 JSON 보고서를 저장한다.
{% endhint %}

## 선행 지식

- 03장의 자료형·자료구조·조건문·반복문·함수·예외 처리를 이해해야 한다.
- 표준 모듈을 `import`하고 Python 파일을 실행할 수 있어야 한다.
- 예제의 `Path.is_relative_to()`를 사용하려면 Python 3.9 이상이 필요하다.

## 학습 우선순위

| 구분 | 내용 |
| --- | --- |
| 필수 | 경로 기준, 작업 범위 검증, 파일 모드, `with`, 인코딩, CSV·JSON 기본 처리 |
| 권장 | 줄 단위 처리, 바이너리 구조, 형식 오류 복구, 임시 파일과 원자적 교체 |
| 심화 | 매직 바이트 식별, 메타데이터·해시 분석, 전체 파일 분석기 설계 |

## 학습 범위와 연결

- 03-2의 문자열·리스트·딕셔너리를 파일 내용과 구조화 데이터에 적용한다.
- 03-4의 반복문을 줄 단위 스트리밍과 여러 레코드 처리에 적용한다.
- 03-5의 함수 계약으로 입력 경로·반환값·파일 변경 범위를 명확히 한다.
- 03-6의 예외 처리로 파일 없음·권한 부족·형식 오류를 구분한다.
- 03-7의 `pathlib`, `csv`, `json`, `hashlib` 같은 표준 모듈을 실제 문제에 사용한다.
- 05장에서는 이 장에서 안전하게 읽은 텍스트를 파싱하고 분석한다.

## 0. 학습 전 확인

다음 질문에 답해 본다.

1. `Path("data/users.txt")`는 어느 위치를 기준으로 해석되는가?
2. `w` 모드로 기존 파일을 열면 원래 내용은 어떻게 되는가?
3. 텍스트의 문자 수와 파일의 바이트 크기는 항상 같은가?
4. CSV와 JSON을 문자열 분리만으로 처리하면 어떤 문제가 생길 수 있는가?
5. 파일을 저장하던 중 프로그램이 중단되어도 이전 파일을 보존하려면 어떤 절차가 필요한가?

답을 확신할 수 없어도 괜찮다. 각 절의 실습을 마친 뒤 같은 질문에 다시 답한다.

## 학습 순서

1. [04-1. 경로·작업 디렉터리·안전한 경로 검증](04-file-io/04-1-paths-filesystem.md)
2. [04-2. 파일·디렉터리 조작과 작업 범위](04-file-io/04-2-filesystem-operations.md)
3. [04-3. 텍스트 파일·모드·with·줄바꿈](04-file-io/04-3-text-files.md)
4. [04-4. 인코딩·bytes와 바이너리 구조](04-file-io/04-4-encoding-binary.md)
5. [04-5. CSV 읽기·검증·안전한 출력](04-file-io/04-5-csv.md)
6. [04-6. JSON·JSON Lines와 직렬화 검증](04-file-io/04-6-json-jsonl.md)
7. [04-7. 대용량 스트리밍과 오류 복구](04-file-io/04-7-streaming-errors.md)
8. [04-8. 임시 파일과 원자적 저장](04-file-io/04-8-safe-output.md)
9. [04-9. 파일 분석기 종합 실습](04-file-io/04-9-file-analyzer.md)

## 형식 선택 기준

파일 확장자만 보고 처리 방법을 정하지 않는다. 데이터의 의미와 교환 규칙을 먼저 확인한다.

| 형식 | Python에서 다루는 값 | 적합한 상황 | 이 장의 핵심 |
| --- | --- | --- | --- |
| 일반 텍스트 | `str` | 로그·설정·사람이 읽는 문서 | 인코딩·줄바꿈·줄 단위 처리 |
| 바이너리 | `bytes` | 이미지·실행 파일·고정 구조 데이터 | 오프셋·바이트 순서·헤더 |
| CSV | 행과 열 | 표 형태의 레코드 교환 | `csv` 모듈·열 검증·`newline=""` |
| JSON | 리스트·딕셔너리 등 | 중첩된 구조화 데이터 | 자료형·필수 키·직렬화 |
| JSON Lines | 한 줄에 JSON 값 하나 | 큰 로그·레코드 스트리밍 | 행 번호·한 건 오류·부분 처리 |

## 이 장에서 연결되는 개념

```text
경로와 작업 범위 검증
→ 파일 생성·복사·이동·삭제
→ 텍스트 모드·인코딩·줄바꿈
→ 바이너리 오프셋·바이트 순서
→ CSV·JSON·JSON Lines 검증
→ 스트리밍과 오류 복구
→ 임시 파일과 원자적 교체
→ 파일 헤더·확장자·메타데이터 비교
→ JSON 분석 보고서 저장
```

각 절의 결과는 마지막 파일 분석기의 한 기능으로 연결된다.

## 학습 방법

1. 예제를 실행하기 전에 생성·변경·삭제될 경로와 출력 결과를 예상한다.
2. 현재 프로젝트가 아닌 별도의 `file-lab` 디렉터리에서 예제를 실행한다.
3. 정상 입력뿐 아니라 빈 값·없는 파일·잘못된 형식 같은 오류 입력도 준비한다.
4. 출력 파일을 다시 읽어 자료형·내용·인코딩이 요구사항과 같은지 확인한다.
5. 원본 입력이 변경되지 않았고 모든 결과가 허용된 작업 영역 안에 있는지 확인한다.
6. 실습이 끝나면 생성한 파일만 정확한 경로를 확인한 뒤 정리한다.

절별 실행 노트북은 [`notebooks/README.md`](notebooks/README.md)의 04장 목록에서 확인할 수 있다.

{% hint style="warning" %}
개인 문서 디렉터리나 프로젝트 전체를 복사·이동·삭제 실습 대상으로 사용하지 않는다. 예제의 경로 검증은 안전한 습관을 익히기 위한 기본 방어선이며, 검사 직후 파일이 바뀌는 경쟁 조건과 운영체제 권한 문제까지 완전히 해결하지는 않는다.
{% endhint %}

## 종합 실습

입력 파일의 기본 정보와 형식별 통계를 JSON으로 저장하는 파일 분석기를 작성한다.

- 파일의 존재 여부와 종류를 확인한다.
- 입력 경로와 출력 경로가 충돌하면 거부한다.
- 크기·수정 시각·SHA-256을 계산한다.
- 파일 헤더를 식별하고 확장자와 비교한다.
- 텍스트와 바이너리를 구분한다.
- 텍스트 행 통계를 스트리밍으로 계산한다.
- CSV와 JSON에 형식별 분석을 적용한다.
- 형식 오류의 위치와 원인을 보고서에 보존한다.
- 고유한 임시 파일에 결과를 쓴 뒤 최종 경로로 원자적으로 교체한다.

{% hint style="success" %}
### ✅ 완료 기준

- [ ] 상대 경로와 절대 경로의 기준 차이를 설명할 수 있다.
- [ ] 허용된 작업 디렉터리 밖의 경로를 거부할 수 있다.
- [ ] 파일을 복사·이동·삭제하기 전에 대상과 충돌 정책을 검증할 수 있다.
- [ ] 작업 목적에 맞는 파일 모드·인코딩·줄바꿈 정책을 선택할 수 있다.
- [ ] 파일 위치와 바이트 순서에 맞게 바이너리 값을 읽을 수 있다.
- [ ] CSV·JSON·JSON Lines에 맞는 표준 모듈을 사용하고 입력 구조를 검증할 수 있다.
- [ ] 큰 파일을 한 줄씩 처리하고 입력 스트리밍과 전체 메모리 사용을 구분할 수 있다.
- [ ] 오류의 원인과 입력 위치를 기록하고 처리 가능한 오류만 복구할 수 있다.
- [ ] 대표적인 파일 헤더와 확장자의 불일치를 확인할 수 있다.
- [ ] 원본을 변경하지 않고 파일 분석 보고서를 생성할 수 있다.
- [ ] 고유한 임시 파일로 결과를 교체하고 저장 결과를 다시 검증할 수 있다.
{% endhint %}

---

다음 장: [05. 텍스트 파싱과 데이터 분석](05-text-processing.md)
