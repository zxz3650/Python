# 06. 네트워크 프로그래밍

파일에 저장된 데이터를 분석하는 단계에서 벗어나, 네트워크를 통해 다른 프로그램과 데이터를 주고받는 방법을 학습합니다. 이 장은 포트 스캔이나 취약점 공격이 아니라 TCP·UDP·이름 해석·메시지 경계·타임아웃 같은 통신의 기본 원리를 코드로 확인하는 과정입니다.

{% hint style="info" %}
## 🧭 학습 목표

- IP 주소·포트·프로토콜·소켓의 관계를 설명합니다.
- TCP 클라이언트와 로컬 테스트 서버를 구현합니다.
- TCP가 메시지가 아닌 바이트 스트림이라는 점을 이해합니다.
- UDP 데이터그램의 특징과 손실 가능성을 설명합니다.
- 도메인 이름을 IPv4·IPv6 주소로 해석합니다.
- 모든 네트워크 작업에 타임아웃과 오류 처리를 적용합니다.
- 길이 접두어 기반 Echo 프로토콜을 완성합니다.
{% endhint %}

## 선행 지식

- 03장의 함수·예외 처리·모듈
- 04장의 `str`·`bytes`·인코딩
- 05장의 데이터 검증과 날짜·시간

## 학습 순서

1. [06-1. 네트워크와 소켓 기초](06-network-programming/06-1-network-socket-basics.md)
2. [06-2. TCP 클라이언트](06-network-programming/06-2-tcp-client.md)
3. [06-3. TCP 서버](06-network-programming/06-3-tcp-server.md)
4. [06-4. 메시지 경계와 프로토콜 설계](06-network-programming/06-4-message-framing.md)
5. [06-5. UDP 통신](06-network-programming/06-5-udp.md)
6. [06-6. DNS와 주소 해석](06-network-programming/06-6-dns-address-resolution.md)
7. [06-7. 타임아웃·오류·재시도](06-network-programming/06-7-timeouts-errors.md)
8. [06-8. 로컬 Echo 통신 프로젝트](06-network-programming/06-8-echo-project.md)

## 이 장에서 연결되는 개념

```text
호스트·IP·포트
→ 소켓 생성
→ 연결 또는 데이터그램 전송
→ bytes 송수신
→ 메시지 경계 복원
→ 응답 검증
→ 타임아웃·오류 처리
→ 정상 종료
```

## 안전한 실습 원칙

{% hint style="warning" %}
- 서버는 기본적으로 `127.0.0.1`에만 바인딩합니다.
- 자신이 소유하거나 명시적으로 허가받은 시스템에만 연결합니다.
- 임의의 주소 범위를 반복해서 탐색하지 않습니다.
- 수신 크기·메시지 길이·연결 시간을 제한합니다.
- 예제 데이터에 비밀번호·토큰·개인정보를 넣지 않습니다.
{% endhint %}

## 학습 방법

1. 서버를 먼저 실행한 뒤 클라이언트를 실행합니다.
2. 송신 전 `str`, 송신 중 `bytes`, 수신 후 `str`의 변화를 확인합니다.
3. 정상 응답뿐 아니라 서버 미실행·타임아웃·잘린 메시지를 시험합니다.
4. 한 번의 `send()`와 `recv()`가 한 메시지를 보장하지 않는 이유를 확인합니다.
5. 모든 소켓을 `with` 문으로 닫습니다.

## 종합 실습

로컬 환경에서 길이 접두어 기반 Echo 서버와 클라이언트를 구현합니다.

- 서버는 `127.0.0.1`의 지정 포트에서 연결을 기다립니다.
- 클라이언트는 UTF-8 메시지의 길이와 본문을 전송합니다.
- 서버는 정확한 길이만큼 수신한 뒤 같은 형식으로 응답합니다.
- 메시지 최대 크기와 타임아웃을 적용합니다.
- 비정상 길이·연결 종료·디코딩 실패를 처리합니다.

{% hint style="success" %}
## ✅ 완료 기준

- [ ] TCP와 UDP의 차이를 설명할 수 있습니다.
- [ ] 클라이언트와 서버의 소켓 생명주기를 설명할 수 있습니다.
- [ ] `str`을 `bytes`로 인코딩해 송수신할 수 있습니다.
- [ ] `sendall()`과 반복 `recv()`가 필요한 이유를 설명할 수 있습니다.
- [ ] 모든 연결에 타임아웃과 입력 크기 제한을 적용할 수 있습니다.
- [ ] 로컬 Echo 클라이언트와 서버를 정상 종료할 수 있습니다.
{% endhint %}

---

다음 장: [07. 시스템 자동화](07-system-automation.md)

