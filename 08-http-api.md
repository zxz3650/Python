# 08. HTTP와 API

HTTP 요청과 응답의 구조를 이해하고 Python으로 웹 API를 호출합니다. 인증정보, 오류, 페이지네이션과 요청 제한을 안전하게 처리합니다.

{% hint style="info" %}
## 🧭 학습 목표

- HTTP 메서드·상태 코드·헤더·본문을 설명합니다.
- `requests`로 GET·POST 요청을 보냅니다.
- 세션·쿠키·토큰을 다룹니다.
- API 응답을 검증하고 저장합니다.
{% endhint %}

## 선행 지식

07장의 네트워크 통신

## 학습 순서

1. 08-1 URL과 HTTP 메시지
2. 08-2 `requests` 기초
3. 08-3 JSON 요청과 응답
4. 08-4 세션·쿠키·인증
5. 08-5 오류·타임아웃·재시도
6. 08-6 페이지네이션과 Rate Limit

## 학습 방법

1. 개념과 용어를 먼저 확인합니다.
2. 최소 예제의 결과를 예측합니다.
3. 정상·오류·경계 입력을 바꾸어 실행합니다.
4. 기능을 작은 함수로 나누어 작성합니다.
5. 종합 실습으로 각 절을 연결합니다.

## 종합 실습

학습용 API의 여러 페이지를 수집하고 오류·요청 횟수·결과를 JSON과 CSV로 저장합니다.

{% hint style="success" %}
## ✅ 완료 기준

- [ ] 모든 요청에 타임아웃을 지정합니다.
- [ ] 상태 코드와 응답 형식을 검증합니다.
- [ ] 인증정보를 소스 코드에 저장하지 않습니다.
{% endhint %}

---

다음 장: [09. 테스트와 디버깅](09-testing-debugging.md)
