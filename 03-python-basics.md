# 03. Python 기초 문법

Python 문법은 단순히 문법 시험을 통과하기 위한 규칙이 아니다. **외부에서 들어온 데이터를 읽고, 의미 있는 정보로 바꾸고, 조건에 따라 판단하고, 결과를 자동으로 기록하는 도구**다. 보안 실무에서는 로그·파일·패킷·명령 실행 결과처럼 형태가 제각각인 데이터를 Python으로 다룬다.

## 이 챕터에서 먼저 알아둘 용어

| 용어 | 쉽게 말하면 | 이 과정에서 하는 일 |
|---|---|---|
| 변수(variable) | 값을 가리키는 이름 | IP, 포트, 로그 한 줄을 이름 붙여 저장 |
| 값(value) | 실제로 다루는 데이터 | `443`, `"DENY"`, `True` 같은 정보 |
| 자료형(type) | 값의 종류와 사용 가능한 연산 | 문자열은 검색하고, 숫자는 계산하고, bytes는 원시 데이터를 처리 |
| 자료구조(data structure) | 여러 값을 목적에 맞게 담는 구조 | 여러 이벤트를 목록·집합·키-값 형태로 정리 |
| 표현식(expression) | 값을 계산하거나 만들어내는 코드 | `failed_count >= 5`처럼 결과를 생성 |
| 문(statement) | 프로그램이 수행할 하나의 명령 | 변수 대입, 조건 판단, 반복, 함수 호출 |
| 함수(function) | 특정 작업을 묶은 재사용 가능한 코드 | 로그 파싱·검증·집계를 분리하고 반복 사용 |
| 예외(exception) | 실행 중 예상하지 못한 상황을 표현하는 객체 | 잘못된 로그를 중단하지 않고 오류 목록으로 보존 |

## 왜 이 개념들을 배우는가

실무 자동화는 다음 흐름으로 이루어진다.

```text
입력 수집 → 값에 이름 붙이기 → 자료형 확인
       → 자료구조로 묶기 → 조건으로 판단
       → 반복 처리 → 함수로 분리 → 결과 저장·검증
```

예를 들어 인증 로그 분석기는 로그 한 줄을 `str`로 읽고, 필드를 `dict`로 구조화한 뒤, `if`로 허용·거부를 판단하고, `for`로 여러 줄을 처리한다. 마지막에는 함수와 예외 처리를 이용해 재사용 가능하고 중단에 강한 프로그램으로 만든다. 각 문법은 따로 존재하는 것이 아니라 이 하나의 문제 해결 흐름을 구성한다.

## 학습 순서

1. [03-1 변수와 자료형](03-python-basics/03-1-data-types.md)
2. [03-2 문자열과 자료구조](03-python-basics/03-2-strings-collections.md)
3. [03-3 조건문과 논리](03-python-basics/03-3-conditions.md)
4. [03-4 반복문: while, True, for](03-python-basics/03-4-loops.md)
5. [03-5 함수와 스코프](03-python-basics/03-5-functions.md)
6. [03-6 파일·JSON·CSV·bytes](03-python-basics/03-6-files-data.md)
7. [03-7 예외·assert·모듈·테스트](03-python-basics/03-7-errors-modules.md)
8. [03-8 인증 로그 분석 미니 프로젝트](03-python-basics/03-8-log-project.md)
9. [03-9 실무 보강 문법](03-python-basics/03-9-practical-syntax.md)

## 학습 방법

각 절의 개념을 읽고 코드 셀을 직접 실행한다. 입력값을 정상·오류·경계값으로 바꾸어 결과를 예측하고 검증한다.

## 최종 산출물

- 실행 가능한 로그 분석기
- 정상·오류·경계값 테스트
- 파싱 오류의 행 번호·원문·원인
- 재현 가능한 JSON 보고서

실습 데이터는 합성 데이터 또는 명시적으로 허가된 데이터만 사용한다.
