# 03. Python 기초 문법

Python 프로그램을 읽고 직접 작성하기 위한 언어의 기본 규칙을 학습한다. 교육용 이벤트 데이터를 일관된 예제로 사용하지만, 목표는 특정 보안 도구가 아니라 값, 흐름, 함수, 오류, 모듈, 객체의 개념을 정확히 이해하고 다른 문제에도 적용하는 것이다.

{% hint style="info" %}
### 🧭 학습 목표

- 값과 자료형에 맞는 연산을 선택한다.
- 자료구조로 여러 값을 표현한다.
- 조건문과 반복문으로 실행 흐름을 제어한다.
- 함수로 코드를 분리하고 결과를 반환한다.
- 예외를 읽고 복구 경계를 설계한다.
- 모듈과 패키지로 코드를 나누고 `python -m`으로 실행한다.
- 클래스와 데이터클래스로 데이터·동작·유효한 상태를 묶는다.
- 정상·오류·경계·상태 시나리오로 프로그램을 검증한다.
{% endhint %}

## 선행 지식

- 02장의 Python 및 가상환경 구성을 완료해야 한다.
- 터미널에서 Python 파일을 실행할 수 있어야 한다.
- 과정의 최소 지원 버전은 Python 3.10이며 Python 3.12를 권장한다.

## 학습 우선순위 사용법

각 절의 `학습 우선순위` 표를 먼저 확인한다.

| 구분 | 의미 | 학습 판단 |
| --- | --- | --- |
| 필수 | 다음 절과 03-9에 직접 필요한 개념 | 설명하고 코드로 구현해야 함 |
| 권장 | 실수 예방과 코드 품질에 중요한 개념 | 예제를 실행하고 완료 기준을 확인 |
| 심화 | 내부 원리나 확장 설계 개념 | 처음에는 읽고 넘어간 뒤 필요할 때 복습 가능 |

필수 내용을 먼저 완성한 뒤 권장·심화 순서로 넓힌다. `심화` 표시는 불필요하다는 뜻이 아니라 첫 학습에서 인지 부하를 조절하기 위한 안내다. 각 절의 `완료 기준`은 권장·심화 내용을 포함한 **최종 도달점**이며, 첫 학습에서 모든 항목을 한 번에 충족해야 한다는 뜻이 아니다.

03-9는 새 개념의 난이도를 구분하지 않는다. 03-1~03-8의 기본 문법을 통합하는 절이므로 `실습 진행 순서`에 따라 핵심 기능 구현과 검증을 완료하고, 확장 과제는 선택 활동으로 분리한다.

## 학습 순서

1. [03-1. 변수와 기본 자료형](03-python-basics/03-1-data-types.md)
2. [03-2. 문자열과 자료구조](03-python-basics/03-2-strings-collections.md)
3. [03-3. 조건문과 논리](03-python-basics/03-3-conditions.md)
4. [03-4. 반복문](03-python-basics/03-4-loops.md)
5. [03-5. 함수와 스코프](03-python-basics/03-5-functions.md)
6. [03-6. 예외 처리](03-python-basics/03-6-exceptions.md)
7. [03-7. 모듈과 패키지](03-python-basics/03-7-modules-packages.md)
8. [03-8. 클래스 기초](03-python-basics/03-8-classes-dataclasses.md)
9. [03-9. 문법 종합 실습](03-python-basics/03-9-practical-syntax.md)

## 학습 방법

각 절은 다음 순서로 학습한다.

1. 용어와 학습 이유를 읽는다.
2. 가장 단순한 예제의 결과를 예측한다.
3. 코드를 실행하고 실제 결과와 비교한다.
4. 오류 예제를 통해 traceback을 읽는다.
5. 보안 이벤트와 다른 도메인의 전이 연습을 해결한다.
6. 접힌 정답은 먼저 해결한 뒤 펼쳐 비교한다.
7. 완료 기준을 결과물과 `assert`로 점검한다.

## 응용 관점의 공통 프레임

문법을 안다는 것은 예제와 같은 코드를 다시 입력하는 데 그치지 않는다. **문제의 상태와 경계를 표현하고, 선택한 구조가 왜 적절한지 설명하며, 실패를 관찰 가능한 결과로 바꾸는 것**까지 포함한다. 각 절의 `응용 인사이트`는 다음 네 질문으로 읽는다.

1. 이 개념은 실제 문제의 어떤 불확실성이나 복잡성을 줄이는가?
2. 다른 문법이나 자료구조 대신 이것을 선택하는 이유는 무엇인가?
3. 입력 크기, 경계값, 변경 가능성, 실패가 달라지면 어디서 깨지는가?
4. 결과가 맞다는 사실을 어떤 출력이나 `assert`로 확인할 수 있는가?

| 절 | 개념을 적용하는 문제 | 설계할 때 던질 핵심 질문 |
| --- | --- | --- |
| 03-1 | 외부 입력을 의미 있는 값으로 변환 | 표현 형식과 업무상 유효 범위를 분리했는가? |
| 03-2 | 여러 값을 검색·중복 제거·연결 | 순서, 중복, 이름표, 변경 가능성 중 무엇이 필요한가? |
| 03-3 | 규칙과 정책을 실행 순서로 변환 | 경계값과 규칙 우선순위, 판단 불가 시 기본 정책은 무엇인가? |
| 03-4 | 같은 규칙을 여러 입력에 적용 | 종료 조건, 누적 상태, 조기 중단, 입력 크기에 따른 비용은 무엇인가? |
| 03-5 | 처리를 재사용·조합·검증 가능한 단위로 분리 | 함수 계약과 부수 효과가 호출자에게 드러나는가? |
| 03-6 | 예상 가능한 실패를 복구 또는 중단으로 분류 | 이 위치에서 실제로 복구할 수 있는가, 원인을 보존하는가? |
| 03-7 | 코드와 의존성의 경계를 나누기 | import 방향과 실행 진입점이 한 방향으로 설명되는가? |
| 03-8 | 데이터의 유효한 상태와 상태 전이를 보호 | 딕셔너리, 데이터클래스, 일반 클래스 중 어느 수준의 규칙이 필요한가? |
| 03-9 | 개념을 관찰 가능하고 시험 가능한 프로그램으로 통합 | 입력부터 결과까지 데이터 흐름과 실패 증거를 추적할 수 있는가? |

서로 다른 응용 문제도 대개 다음 흐름으로 환원할 수 있다.

```text
외부 표현 → 변환·검증 → 구조화된 값 → 조건 판단
          → 반복 처리 → 함수·객체의 상태 변화 → 결과·오류 증거
```

예를 들어 로그인 이벤트 분석, 재고 점검, 센서 이상 탐지에는 서로 다른 데이터가 등장하지만 위 흐름과 필요한 Python 문법은 같다. 따라서 예제의 도메인 이름을 외우기보다 **입력, 규칙, 상태, 출력의 역할**을 찾아 다른 문제로 옮겨 본다.

## 종합 실습

03-9에서는 **메모리 기반 이벤트 검토 큐**를 구현한다.

- `add`, `list`, `find`, `summary`, `remove`, `quit` 명령 제공
- 문자열 명령을 검증된 값 객체로 변환
- 저장소 객체로 목록·검색·집계·삭제 상태 관리
- 잘못된 입력을 명령 단위로 복구
- 파싱·상태·출력·입력을 함수와 모듈로 분리
- 입력·출력 주입과 자동 시나리오로 전체 흐름 검증
- 같은 구조를 연락처·도서·작업·재고 문제에 전이

완성 프로젝트는 [03-9. 문법 종합 실습](03-python-basics/03-9-practical-syntax.md)의 인수 조건과 100점 루브릭으로 평가한다. 파일 저장과 네트워크 통신은 다음 장부터 학습한다.

## 완료 기준

- [ ] 기본 자료형과 자료구조를 구분할 수 있다.
- [ ] 조건문과 반복문의 실행 순서를 설명할 수 있다.
- [ ] 입력과 반환값이 있는 함수를 작성할 수 있다.
- [ ] traceback에서 오류 유형과 발생 위치를 찾을 수 있다.
- [ ] 작성한 코드를 모듈과 패키지로 분리해 `python -m`으로 실행할 수 있다.
- [ ] 데이터클래스와 저장소 객체로 유효한 상태를 표현할 수 있다.
- [ ] 정상·오류·경계·상태 시나리오를 자동으로 다시 실행할 수 있다.
- [ ] 같은 문법과 구조를 보안 이벤트가 아닌 다른 도메인에 적용할 수 있다.

---

다음 장: [04. 파일 입출력과 데이터 형식](04-file-io.md)
