# 04-4. CSV 읽기와 쓰기

CSV는 행과 열로 구성된 표 형식입니다. 값 안에 쉼표·줄바꿈·따옴표가 들어갈 수 있으므로 `split(",")` 대신 표준 `csv` 모듈을 사용합니다.

{% hint style="info" %}
## 🧭 학습 목표

- `csv.reader`와 `csv.DictReader`를 구분합니다.
- 문자열 필드를 필요한 자료형으로 변환합니다.
- 오류 행의 위치와 원인을 기록합니다.
- `DictWriter`로 새로운 CSV를 작성합니다.
{% endhint %}

## 선행 지식

텍스트 파일과 리스트·딕셔너리를 이해해야 합니다.

## 1. CSV 읽기

```python
import csv
from pathlib import Path

path = Path("people.csv")

with path.open("r", encoding="utf-8", newline="") as file:
    reader = csv.reader(file)
    for row in reader:
        print(row)
```

각 행은 문자열 리스트로 반환됩니다. `newline=""`은 CSV 모듈이 줄바꿈을 직접 처리하도록 합니다.

## 2. DictReader

```python
records = []

with path.open("r", encoding="utf-8", newline="") as file:
    reader = csv.DictReader(file)
    for row in reader:
        records.append(row)

print(records[0]["name"])
```

헤더가 키가 되므로 각 필드의 의미를 이름으로 확인할 수 있습니다.

## 3. 자료형 변환과 검증

CSV에서 읽은 모든 필드는 기본적으로 문자열입니다.

```python
valid_records = []
errors = []

with path.open("r", encoding="utf-8", newline="") as file:
    reader = csv.DictReader(file)

    for line_number, row in enumerate(reader, start=2):
        try:
            age = int(row["age"])
            if age < 0:
                raise ValueError("나이는 0 이상이어야 합니다")
        except (ValueError, KeyError) as exc:
            errors.append({
                "line": line_number,
                "raw": row,
                "error": str(exc),
            })
            continue

        row["age"] = age
        valid_records.append(row)
```

헤더가 1행이므로 데이터 행 번호는 2부터 시작합니다.

## 4. 쉼표가 포함된 값

```python
from io import StringIO

text = 'name,note\nAlice,"Seoul, Korea"\n'
reader = csv.DictReader(StringIO(text))

print(next(reader))
```

CSV 모듈은 따옴표로 감싼 쉼표를 하나의 필드로 처리합니다.

## 5. CSV 쓰기

```python
fields = ["name", "age", "city"]

with Path("output.csv").open(
    "w",
    encoding="utf-8",
    newline="",
) as file:
    writer = csv.DictWriter(file, fieldnames=fields)
    writer.writeheader()
    writer.writerows(valid_records)
```

출력 열과 순서를 `fieldnames`로 명시합니다.

## 6. UTF-8 BOM

스프레드시트 프로그램과의 호환이 필요하면 `utf-8-sig`를 사용할 수 있습니다.

```python
with Path("excel_output.csv").open(
    "w",
    encoding="utf-8-sig",
    newline="",
) as file:
    writer = csv.DictWriter(file, fieldnames=fields)
    writer.writeheader()
    writer.writerows(valid_records)
```

## 흔한 실수

- 쉼표로 직접 문자열을 분리함
- 숫자 필드를 문자열 상태로 계산함
- 헤더 누락을 확인하지 않음
- 실제 파일 행 번호를 잘못 기록함
- `newline=""`을 생략함

{% hint style="success" %}
## 🧪 종합 실습

상품명·가격·수량 CSV를 읽고 숫자 변환과 범위 검증을 수행합니다. 정상 행에는 합계 금액을 추가해 새 CSV로 저장하고 오류 행은 별도로 보존합니다.
{% endhint %}

## 완료 기준

- [ ] 표준 CSV 모듈로 파일을 읽고 쓸 수 있습니다.
- [ ] 문자열 필드를 업무 자료형으로 변환할 수 있습니다.
- [ ] 오류 행의 번호·원문·원인을 보존할 수 있습니다.

---

다음 절: [04-5. JSON 직렬화](04-5-json.md)
