# 07. HTTP와 API

![HTTP는 요청과 응답의 규칙, JSON은 본문의 데이터 형식, Python은 이를 해석하고 검증하는 도구](assets/07-http-api-overview.svg)

06장에서는 소켓으로 바이트를 주고받았습니다. 이제 **“서버에 무엇을 요청했고, 받은 결과를 어떻게 읽을 것인가?”**를 배웁니다. 같은 바이트라도 요청 경로, 처리 결과, 데이터 형식에 대한 약속이 있어야 프로그램끼리 의미를 이해할 수 있습니다. 이 약속을 HTTP와 API로 살펴봅니다.

{% hint style="info" %}
### 이 장을 끝내면

로컬 서버의 상태를 요청하고, 응답에서 상태 코드·헤더·본문을 구분한 뒤, 필요한 값을 검증해 점검 보고서로 저장할 수 있습니다.
{% endhint %}

## 1. 먼저 구분할 세 가지

| 개념 | 답하는 질문 | 이 장의 예 |
| --- | --- | --- |
| **HTTP** | 어떤 방식으로 요청하고 응답하는가? | `GET /health`, 상태 코드 `200`, 응답 헤더 |
| **API** | 어떤 경로에 무엇을 보내고 어떤 결과를 받기로 했는가? | `/health`는 서비스 상태와 이름을 반환 |
| **JSON** | 본문의 데이터를 어떤 문법으로 표현하는가? | `{"status": "ok", "service": "python-basic-lab"}` |

여기서 API는 **HTTP를 통해 사용하는 웹 API**를 뜻합니다. API라는 말 자체는 라이브러리의 함수 인터페이스 등 더 넓은 범위에서도 사용됩니다.

HTTP 본문에는 JSON뿐 아니라 HTML, 이미지, 일반 텍스트도 담을 수 있습니다. 본문이 없는 응답도 있습니다. 따라서 HTTP를 배운다는 것이 모든 응답을 JSON으로 처리한다는 뜻은 아닙니다.

## 2. HTTP 수업에서 왜 JSON을 다룰까요?

브라우저로 상태 페이지를 보면 사람은 화면의 “정상”이라는 글을 읽을 수 있습니다. Python 자동화에는 **어떤 값이 서비스 상태이고 어떤 값이 서비스 이름인지** 구분할 수 있는 구조가 필요합니다. 이 장의 서버는 이름과 값을 묶는 JSON 형식으로 응답합니다.

```json
{"status": "ok", "service": "python-basic-lab"}
```

Python은 이 본문을 `dict`로 바꾼 뒤 `data["status"]`로 필요한 값에 접근합니다. 화면 문구의 위치를 찾는 대신 필드 이름으로 데이터를 읽는 것입니다. 이 과정이 서버 상태 확인, 점검 결과 수집, 보고서 자동화의 기반이 됩니다.

| 앞에서 배운 내용 | 07장에서 이어지는 작업 |
| --- | --- |
| 03장: `dict`, `list`, 조건문, 예외 처리 | 응답 필드를 읽고 잘못된 값을 구분 |
| [04-6: JSON·JSON Lines](04-file-io/04-6-json-jsonl.md) | 파일에서 읽던 JSON을 HTTP 응답 본문에서도 읽기 |
| 05장: 자료형·필수 값 검증 | 파싱된 응답이 API의 약속을 지키는지 확인 |
| 06장: TCP·포트·타임아웃 | 연결 실패와 HTTP 처리 실패를 구분 |

**JSON 문법을 읽을 수 있다는 것과 응답을 신뢰할 수 있다는 것은 별도 조건입니다.** 오류 응답도 JSON일 수 있고, JSON 문법이 올바르더라도 필수 값이 빠질 수 있습니다. 그래서 이 장에서는 호출 뒤의 검증까지 함께 배웁니다.

## 3. 학습 경로

| 단계 | 핵심 질문 | 직접 확인할 산출물 |
| --- | --- | --- |
| [07-1. URL과 HTTP 메시지](07-http-api/07-1-url-http-messages.md) | 어디로 무엇을 요청하는가? | URL과 요청·응답을 설명한 표 |
| [07-2. requests 기초](07-http-api/07-2-requests-basics.md) | Python에서 응답을 어떻게 받는가? | 상태·헤더·본문 출력 |
| [07-3. JSON API와 응답 검증](07-http-api/07-3-json-api.md) | 본문을 어떻게 데이터로 읽는가? | 정상·오류 입력을 구분하는 검증 함수 |
| [07-4. 세션·쿠키·인증](07-http-api/07-4-sessions-cookies-auth.md) | 여러 요청의 사용자를 어떻게 연결하는가? | Session과 로그인 세션의 차이 설명 |
| [07-5. 오류·타임아웃·재시도](07-http-api/07-5-errors-timeouts-retries.md) | 실패하면 중단할까, 다시 보낼까? | 원인별 처리와 자원 제한 |
| [07-6. HTTP 보안 검증 기초](07-http-api/07-6-http-security-validation.md) | 어떤 관찰을 근거로 남길까? | 관찰값·판정·한계를 나눈 기록 |
| [07-7. 로컬 네트워크·웹 보안 점검 프로젝트](07-http-api/07-7-local-web-security-project.md) | 배운 내용을 어떻게 하나로 연결할까? | 로컬 점검 JSON 보고서 |

## 4. 실습 환경을 먼저 준비합니다

저장소 루트에서 의존성을 설치합니다. 터미널 A는 서버를 계속 실행하고, 터미널 B에서 Python 예제를 실행합니다.

```bash
python -m pip install -r requirements.txt
python examples/07-local-web-security-lab/training_server.py
```

예상 시작 메시지:

```text
Training server: http://127.0.0.1:8080
Press Ctrl+C to stop
```

| 경로 | 제공 기능 | 학습 목적 |
| --- | --- | --- |
| `/health` | 서비스 상태 JSON, `200` | 상태·본문 읽기와 계약 검증 |
| `/api/echo?text=hello` | 전달한 문자열과 길이 JSON, `200` | 쿼리 매개변수 이해 |
| `/headers` | 일부 보안 헤더가 없는 JSON, `200` | 헤더 관찰 연습 |
| `/redirect` | `/health`로 이동 안내, `302` | 자동 이동과 목적지 확인 |
| `/missing` | 없는 경로의 오류 JSON, `404` | HTTP 오류와 JSON 파싱 구분 |

이 서버는 GET 실습용입니다. POST에는 `405`를 반환하고 로그인·페이지네이션 기능은 제공하지 않습니다. 해당 주제는 각 절에서 **전송 없이 관찰하는 예제** 또는 **개념 설명**으로 구분합니다.

서버에 연결되지 않으면 서버 터미널이 실행 중인지와 포트가 같은지 확인합니다. 8080번 포트를 이미 쓰고 있다면 `--port 8081`로 시작하고 예제 URL의 포트도 함께 바꿉니다. 실습을 마치면 터미널 A에서 `Ctrl+C`로 종료합니다.

## 5. 실습 범위와 판정 원칙

{% hint style="warning" %}
실습은 제공 서버의 `127.0.0.1` 주소에서 진행합니다. 실제 토큰·비밀번호·쿠키를 사용하거나 기록하지 않습니다. 점검 결과는 관찰된 조건을 뜻하며, 상태 코드나 헤더 하나로 취약점·침해를 확정하지 않습니다.
{% endhint %}

이 장은 TCP 기반 HTTP 통신으로 설명합니다. 실제 HTTP에는 다른 전송 방식도 있으므로 모든 버전이 동일한 계층 구성을 쓴다고 일반화하지 않습니다. HTTPS의 인증서 검증은 유지하며 로컬 HTTP 예제와 운영 HTTPS 환경을 구분합니다.

{% hint style="success" %}
### ✅ 장 마무리 기준

- [ ] HTTP, 웹 API, JSON의 역할을 각각 설명합니다.
- [ ] 상태 코드·헤더·본문을 Python에서 구분해 읽습니다.
- [ ] HTTP 상태 확인, JSON 파싱, 데이터 검증이 다른 단계임을 설명합니다.
- [ ] 연결·읽기 타임아웃과 응답 크기를 제한합니다.
- [ ] 관찰 근거와 판정의 한계를 보고서에 남깁니다.
{% endhint %}

---

시작: [07-1. URL과 HTTP 메시지](07-http-api/07-1-url-http-messages.md) · 다음 장: [08. 시스템 자동화](08-system-automation.md)
