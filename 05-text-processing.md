# 05. 텍스트 파싱과 데이터 분석

파일에서 읽은 문자열을 정규화하고 필요한 필드를 추출·검증한 뒤, NumPy와 pandas로 구조화된 데이터를 집계하는 방법을 학습한다. 마지막에는 웹 접근 로그를 스트리밍하며 데이터 품질과 조사 후보를 함께 검증하는 종합 실습을 완성한다.

{% hint style="info" %}
### 🧭 학습 목표

- 원문을 보존하면서 비교·검색용 텍스트를 정규화한다.
- 문자열 메서드와 정규표현식의 선택 기준을 설명한다.
- 패턴에서 필요한 필드를 추출하고 형식·의미·범위를 검증한다.
- 날짜와 시간을 시간대가 있는 `datetime`으로 변환하고 UTC로 통일한다.
- NumPy 배열과 pandas DataFrame의 역할과 선택 기준을 구분한다.
- 인코딩·파싱·검증 오류를 구분하고 처리 건수를 대조한다.
- 민감정보를 최소화하고 가명처리한 결과를 전용 출력 영역에 안전하게 저장한다.
{% endhint %}

## 선행 지식

- 03장의 문자열·리스트·딕셔너리·함수·예외 처리를 사용할 수 있어야 한다.
- 04장의 경로 검증, 인코딩, CSV·JSONL, 스트리밍, 안전한 출력을 이해해야 한다.
- 02장의 가상환경에서 `requirements.txt`를 설치한 뒤 저장소 루트에서 JupyterLab을 실행한다.

## 학습 순서

| 순서 | 절 | 핵심 산출물 | 05-9와의 연결 |
| --- | --- | --- | --- |
| 1 | [05-1. 문자열 정규화](05-text-processing/05-1-normalization.md) | 원문·정규화 값 쌍 | 줄바꿈과 유니코드 처리 |
| 2 | [05-2. 분리·검색·치환](05-text-processing/05-2-split-search-replace.md) | 고정 형식 레코드 | 요청 문자열 분리 |
| 3 | [05-3. 정규표현식 기초](05-text-processing/05-3-regex-basics.md) | 작은 패턴과 실패 사례 | Combined Log 형식 일치 |
| 4 | [05-4. 그룹과 캡처](05-text-processing/05-4-groups-capture.md) | 이름 있는 필드 | 로그 필드 추출 |
| 5 | [05-5. 데이터 검증](05-text-processing/05-5-validation.md) | 정상 레코드·구조화된 오류 | IP·상태 코드 의미 검증 |
| 6 | [05-6. 날짜와 시간](05-text-processing/05-6-datetime.md) | UTC `datetime` | 5분 시간창 집계 |
| 7 | [05-7. NumPy 배열과 대용량 수치 처리](05-text-processing/05-7-numpy-array.md) | 벡터 연산·조건 마스크 | 탐지 조건을 배열로 계산 |
| 8 | [05-8. pandas와 DataFrame 기반 대용량 처리](05-text-processing/05-8-pandas-dataframe.md) | 필터·집계·청크 결과 | 시간창·IP별 특징표 |
| 9 | [05-9. 웹 접근 로그 분석 종합 실습](05-text-processing/05-9-web-log-analysis.md) | 품질 보고서·조사 후보·마스킹된 산출물 | 05장 통합 |

05-7은 별도의 수치 이론 챕터가 아니라, 05-9의 다중 조건 탐지를 조건 마스크와 `np.select()`로 계산하기 위한 배경이다. pandas만 사용해도 되는 작은 일회성 표 처리와 NumPy 배열 연산이 필요한 경우를 구분한다.

## 절별 학습 구조

각 절은 다음 순서를 따른다.

```text
학습 전 확인
→ 개념과 선택 기준
→ 오류·경계 사례
→ fixture 기반 실습
→ 자기점검
→ 응용 인사이트
```

학습자용 Notebook에는 TODO와 공개 경계 테스트만 둔다. 풀이 검증용 Notebook은 자신의 구현을 먼저 완성한 뒤 비교하는 참고 구현이다. 공개 저장소의 풀이 파일은 평가 정답을 숨기지 못하므로, 실제 평가에서는 교수자용 비공개 테스트를 별도로 운영한다.

- 학습자용: [`notebooks/`](notebooks/README.md)
- 풀이 검증용: [`notebooks/solutions/README.md`](notebooks/solutions/README.md)
- 실습 입력: [`fixtures/05-text-processing/README.md`](fixtures/05-text-processing/README.md)

## 데이터 처리 원칙

1. **원문과 파생 값을 구분한다.** 정규화·마스킹·경로 표준화 값을 새 필드로 만든다.
2. **인코딩·형식·의미 오류를 나눈다.** 서로 다른 해결 방법과 품질 지표를 갖는다.
3. **파싱 결과와 검증 결과를 나눈다.** 패턴이 맞는 IP 문자열이 유효한 IP 주소라는 보장은 없다.
4. **크기를 확인한 뒤 메모리 전략을 정한다.** 청크가 상세 행의 메모리를 제한해도, 고유 IP·경로 집합은 계속 커질 수 있다.
5. **집계를 해석하기 전에 불변식을 검증한다.** 전체·정상·오류 건수와 집계 합계가 서로 맞아야 한다.
6. **민감정보를 결과에 반복하지 않는다.** IP는 가명처리하고 쿼리·토큰·세션 값과 오류 원문은 산출물에서 제외한다.
7. **출력 경로와 저장 단계를 검증한다.** 전용 루트 밖으로 나가지 않게 하고 임시 파일을 완성한 뒤 원자적으로 교체한다.

{% hint style="warning" %}
이 장의 로그·IP·민감 경로 사례는 문서용 주소와 결정론적 합성 데이터를 사용한다. 실제 조직 로그는 승인·보존·반출·개인정보 정책을 확인한 환경에서만 처리한다.
{% endhint %}

## 종합 실습 산출물

05-9에서는 Apache/Nginx Combined Log 형식의 웹 로그를 다음 순서로 처리한다.

```text
바이트 줄 읽기
→ UTF-8 strict 디코딩
→ 형식 파싱
→ IP·상태·시간 의미 검증
→ URL 경로 파생 값 생성
→ 5분 시간창 특징 집계
→ NumPy 조건 마스크로 조사 후보 선별
→ 민감정보 가명처리
→ 품질·후보·매니페스트 안전 저장
```

후보 규칙은 교육용 희스틱이며 침해 판정이 아니다. 정상 행위와 오탐 사례를 함께 두고, 신호·조사 맥락·판정을 나눠 기록한다.

## 완료 기준

- [ ] 문자열 메서드와 정규표현식을 문제에 맞게 선택할 수 있다.
- [ ] 원문·정규화·가명처리 값의 역할을 구분할 수 있다.
- [ ] 이름 있는 그룹으로 값을 추출하고 형식과 의미를 따로 검증할 수 있다.
- [ ] 시간대가 있는 날짜를 UTC로 변환할 수 있다.
- [ ] NumPy 마스크와 pandas 집계를 각각 적절한 위치에 사용할 수 있다.
- [ ] 전체 건수와 오류 유형별 건수의 불변식을 검증할 수 있다.
- [ ] 배치 메모리와 고유값 집계 메모리의 차이를 설명할 수 있다.
- [ ] 조사 후보와 침해 확정을 구분하고 판정에 필요한 맥락을 제시할 수 있다.
- [ ] 마스킹된 분석 결과를 검증된 출력 경로에 안전하게 저장할 수 있다.

---

다음 장: [06. 네트워크 프로그래밍](06-network-programming.md)
