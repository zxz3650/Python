# 05. 텍스트 파싱과 데이터 분석

파일에서 읽은 문자열을 정규화하고 필요한 필드를 추출·검증한 뒤, NumPy와 pandas로 구조화된 데이터를 집계하는 방법을 학습합니다. 마지막에는 대용량 웹 로그를 배치 단위로 처리하는 분석 Notebook을 완성합니다.

{% hint style="info" %}
## 🧭 학습 목표

- 원문을 보존하면서 텍스트를 정규화합니다.
- 문자열 메서드와 정규표현식의 선택 기준을 설명합니다.
- 패턴에서 필요한 필드를 추출하고 값을 검증합니다.
- 날짜와 시간을 UTC 같은 공통 기준으로 변환합니다.
- NumPy 배열의 구조와 벡터 연산을 이해합니다.
- pandas DataFrame으로 표 데이터를 필터링하고 집계합니다.
- 대용량 웹 로그를 배치 DataFrame으로 분석합니다.
{% endhint %}

## 선행 지식

- 04장의 파일 분석기와 스트리밍 처리를 완료해야 합니다.
- 리스트·딕셔너리·함수·예외 처리를 사용할 수 있어야 합니다.

## 학습 순서

1. [05-1. 문자열 정규화](05-text-processing/05-1-normalization.md)
2. [05-2. 분리·검색·치환](05-text-processing/05-2-split-search-replace.md)
3. [05-3. 정규표현식 기초](05-text-processing/05-3-regex-basics.md)
4. [05-4. 그룹과 캡처](05-text-processing/05-4-groups-capture.md)
5. [05-5. 데이터 검증](05-text-processing/05-5-validation.md)
6. [05-6. 날짜와 시간](05-text-processing/05-6-datetime.md)
7. [05-7. NumPy 배열과 대용량 수치 처리](05-text-processing/05-7-numpy-array.md)
8. [05-8. pandas와 DataFrame 기반 대용량 처리](05-text-processing/05-8-pandas-dataframe.md)
9. [05-9. 웹 로그 분석](05-text-processing/05-9-web-log-analysis.md)

## 이 장에서 연결되는 개념

```text
원문 입력
→ 문자열 정규화
→ 패턴 일치와 필드 추출
→ 형식·의미 검증
→ 날짜·시간 통일
→ NumPy 수치 처리
→ DataFrame 배치 집계
→ 웹 로그 분석 보고서
```

## 학습 방법

1. 먼저 문자열 메서드로 해결할 수 있는지 판단합니다.
2. 정규표현식은 작은 패턴부터 만들고 실패 입력도 함께 검증합니다.
3. 파싱 결과와 유효성 검증 결과를 구분합니다.
4. 배열과 DataFrame의 구조·자료형·메모리 사용량을 확인합니다.
5. 큰 파일은 전체가 아니라 배치 단위로 처리합니다.
6. 집계값을 해석하기 전에 파싱 성공·실패 건수를 대조합니다.

## 종합 실습

Apache/Nginx Combined Log 형식의 웹 로그를 분석합니다.

- 원문을 한 줄씩 읽고 정규표현식으로 필드를 추출합니다.
- timestamp를 UTC로 변환합니다.
- 일정한 행 수만 DataFrame으로 만들어 집계합니다.
- 상태 코드·메서드·IP·경로·시간대별 통계를 생성합니다.
- 반복 404와 민감 경로 요청을 후속 조사 후보로 분류합니다.
- 결과와 데이터 품질 정보를 CSV·JSON으로 저장합니다.

{% hint style="success" %}
## ✅ 완료 기준

- [ ] 문자열 메서드와 정규표현식을 선택할 수 있습니다.
- [ ] 이름 있는 그룹으로 값을 추출할 수 있습니다.
- [ ] 형식 검증과 의미 검증을 구분할 수 있습니다.
- [ ] 시간대가 있는 날짜를 UTC로 변환할 수 있습니다.
- [ ] NumPy 배열과 pandas DataFrame의 역할을 설명할 수 있습니다.
- [ ] 대용량 웹 로그를 전체 메모리 적재 없이 집계할 수 있습니다.
- [ ] 분석 후보와 침해 확정을 구분해 설명할 수 있습니다.
{% endhint %}

---

다음 장: [06. 시스템 자동화](06-system-automation.md)
